{
  "name": "the-sushi",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host --port 3000",
    "start": "vite preview --host --port 3000",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
